{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run EduManager App (Flask)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal"
        }
    ]
}
